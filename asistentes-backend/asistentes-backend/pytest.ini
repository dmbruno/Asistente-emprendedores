[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra --strict-markers --tb=short
markers =
    integration: requiere Supabase real (skip en CI por defecto)
    afip: requiere certificado AFIP (skip si no está cargado)
